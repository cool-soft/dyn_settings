from setuptools import setup, find_packages

setup(
    name='dynamic_settings',
    version='',
    url='',
    license='',
    author='Georgij',
    author_email='',
    description='',
    zip_safe=False,
    packages=find_packages(where='src'),
    package_dir={'': 'src'},
)
